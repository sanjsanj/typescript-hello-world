module demo_dynamic_types {
    var person: string;

    person = "Bob Bobson";
    console.log(person.substring(1, 4));

    //person = {
    //    name: "Tim Timson",
    //    age: 25
    //};
    //console.log(person.name.substring(1, 4));
}